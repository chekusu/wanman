{
  "name": "wanman-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent Matrix framework — open-source runtime, CLI, and SDK for autonomous agent collaboration.",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:coverage": "vitest run --coverage --coverage.reporter=text-summary --coverage.reporter=json-summary --coverage.exclude=**/dist/**",
    "clean": "turbo clean && node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('node_modules', { recursive: true, force: true });\""
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "tsx": "^4.21.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=20.0.0" }
}
